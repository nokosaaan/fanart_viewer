Django>=4.2
djangorestframework
psycopg2-binary
requests
playwright
beautifulsoup4
yt-dlp
python-dotenv
gallery-dl
gunicorn
whitenoise
google-auth
google-api-python-client
cryptography
# Pinned: 1.23+ dropped cp310 wheels entirely (Python 3.10, which the
# Playwright base image may still use) — 1.22.0 still covers cp310-cp313
# on both x86_64 and aarch64 (Pi4), so pin rather than risk pip falling
# back to a source build with no compatible wheel.
onnxruntime==1.22.0
pillow
# dghs-imgutils (item/tagger.py's _detect_person_boxes, an anime-art person
# detector used to separate multiple characters into individual regions
# before tagging) is installed separately via `pip install --no-deps` (see
# Dockerfile) rather than as a plain line here — one of its OWN hard
# dependencies, bchlib, has no aarch64 (Pi4) wheel on PyPI and fails to
# build from source in this image, breaking the build entirely (confirmed:
# a plain `pip install dghs-imgutils` build fails on aarch64 for this
# reason). bchlib is used only by an unrelated submodule of imgutils
# (LSB steganography, imgutils/metadata/lsb) that item.tagger never
# imports — verified live that `from imgutils.detect import detect_person`
# and a real inference call both work with bchlib completely absent. The
# lines below are dghs-imgutils's own actual dependencies (from its PyPI
# metadata) MINUS bchlib (skipped) and pyrfc6266 (see next comment — also
# installed via --no-deps), so `--no-deps` on those two doesn't silently
# leave anything else missing. If dghs-imgutils's own requirements ever
# change, re-check these against its metadata (PyPI JSON API).
hbutils>=0.9.0
scikit-learn
huggingface-hub
tqdm
opencv-contrib-python
pandas
scipy
emoji<2.12,>=2.5.0
pilmoji>=1.3.0
shapely!=2.0.7
pyclipper
deprecation>=2.0.0
hfutils>=0.9.0
filelock
piexif
urlobject>=2
tokenizers>=0.20.0
numpy<2
# pyrfc6266 (dghs-imgutils's dependency for parsing download filenames) is
# ALSO installed via --no-deps (see Dockerfile) rather than as a plain line
# here: its own metadata declares pyparsing~=3.0.7, which pip's resolver
# refuses to reconcile with the pyparsing>=3.1 pin below (needed by
# httplib2/google-api-python-client, for the Drive backup feature) even
# though pyrfc6266 actually runs fine against pyparsing 3.1+ (verified live).
# Its only real dependency is pyparsing, which is pinned explicitly below.
pyparsing>=3.1,<4
# Dev/analysis tool only (item/management/commands/evaluate_tag_count.py) —
# not on any request-serving code path. Headless (Agg backend), portable
# wheels on both x86_64 and aarch64.
matplotlib
# Katakana/kana -> romaji conversion, used only by danbooru_lookup.py's
# title-roster character matching (item/management/commands/
# audit_character_aliases.py) to romanize this app's own Japanese given
# names for fuzzy comparison against Danbooru's own romanized character
# tags. Pure Python + a small built-in kana table — no ML model, no native
# extension, trivial on the Pi4 deployment target.
pykakasi